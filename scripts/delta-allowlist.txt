# SLU fork delta allowlist — the budgeted difference vs the upstream baseline
# (upstream/fedora44). Enforced by scripts/upstream-delta.sh + fork CI.
#
# B2 rules (memory-bank/projectRules.md#Fork-Maintenance):
#   - [modified]           upstream files we modify. Every hunk must be
#                          distro-guarded and a no-op on the baseline.
#                          ADDING a path here requires justification in the
#                          same PR; this file's git history IS the delta audit.
#   - [intentionally-absent] baseline files we deliberately do not ship.
#   - [generated]          never hand-edit; always take baseline verbatim.
#                          (README/Jenkinsfile/package_versions are bot-maintained.)
#
# Guard predicate rule: predicates must be content-based
# (/etc/redhat-release, file-exists, binary-exists). 'dnf' is NOT a
# discriminator (present on fedora AND UBI9). Test every guard against
# trixie / f44 / UBI9 fixtures.

[modified]
# Dockerfile: WHOLE-FILE OWNERSHIP (not a distro-guarded hunk) — the branch's
# MAIN image is the SLU RHEL9 image. Upstream design: the branch name implies
# the variant, so the build file is just `Dockerfile` (was `Dockerfile.rhel9`,
# renamed 2026-09-01). Supersedes the baseline fedora44 content at this path
# (still reachable via upstream/fedora44 — nothing lost).
Dockerfile
root/defaults/startwm.sh
root/etc/s6-overlay/s6-rc.d/init-nginx/run
root/etc/s6-overlay/s6-rc.d/init-selkies-config/run
root/etc/s6-overlay/s6-rc.d/svc-docker/run
# svc-xorg/run: M3 (2026-09-03) adds a guarded Xorg + NVIDIA DDX branch above
# the Xvfb exec. Guard predicate = env-marker + file-exists (both content
# based, per projectRules guard rule). On upstream fedora44 nothing sets
# XSERVER_BACKEND=nvidia-xorg and /usr/local/bin/selu-xorg-config is not in
# the tree (that path is a SLU additive file — see the [additive] audit) —
# the guard is false, the pre-existing Xvfb exec runs unchanged. When M3 is
# enabled but the config script declines (no NVIDIA runtime, download fail,
# etc.), we log + fall through to the same Xvfb exec. See findings F58 +
# decisions 2026-08-31 (GPU driver mismatch — boot-time userspace install).
root/etc/s6-overlay/s6-rc.d/svc-xorg/run
# svc-selkies/run: M3 (2026-09-08) adds a 1-line wait-for-X-socket before the
# selkies exec. M3 Xorg+NVIDIA takes 2-4 min for boot-time module download;
# without this wait, pixelflux starts before the X display exists and falls
# back to a 1024x768 virtual default (no stream). On upstream fedora44 (Xvfb),
# the socket appears instantly so the loop exits on the first iteration (no-op).
root/etc/s6-overlay/s6-rc.d/svc-selkies/run
root/selkies-proot

# Intentionally absent:
#   Dockerfile.aarch64 — deleted 2026-09-01 (user decision): a fedora arm64
#     build file next to the RHEL9 one is confusing; this branch ships amd64
#     only for now. Re-add (as [modified]) when RHEL9 arm64 work starts.
#     NOTE: the baseline Jenkinsfile (generated, taken verbatim) still
#     references Dockerfile.aarch64 — harmless: this branch is built locally
#     (podman), never by LS Jenkins.
#   The 6 svc-dbus files (absent on f44, present on the old master baseline)
#   ARE re-added here as ADDITIVE files — f44's deletion is fatal for our
#   GNOME 40 desktop: ui/status/power.js:39 probes the SYSTEM bus at startup
#   and throws a fatal JS exception without /run/dbus/system_bus_socket (f44
#   runs openbox, so upstream never hit it). Reconcile QA regression, fixed
#   2026-09-01.
[intentionally-absent]
Dockerfile.aarch64

[generated]
Jenkinsfile
README.md
package_versions.txt
jenkins-vars.yml
.github/workflows/external_trigger.yml
.github/workflows/greetings.yml
